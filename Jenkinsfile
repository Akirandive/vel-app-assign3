
pipeline{
       agent {
           label {
            lable "slave-1"
            customWorkspace "/mnt/assign3"
           }
       }

    stages {
          stage("slave1 deployment")
          {
            steps{
                sh "clone "
            }
          }

    }
}
